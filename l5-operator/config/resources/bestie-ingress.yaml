apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bestie-ingress
spec:
  rules:
  - host: pets.bestie.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          serviceName: bestie-service
          servicePort: 8000
